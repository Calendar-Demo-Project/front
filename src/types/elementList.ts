export interface ITask {
  type: string;
  text: string;
  active: boolean;
  color: string;
}

export interface IEvent {
  type: string;
  text: string;
  active: boolean;
  color: string;
}

export interface ICategory {
  type: string;
  text: string;
  color: string;
}
